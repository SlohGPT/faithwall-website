# TODO — Karol

Items the SEO autopilot can't do itself. Newest first.

## 2026-08-31 (weekly refresh)

- **Nothing new is required from you this week.** The four open decisions below are unchanged and still yours: the Cloudflare robots.txt override (08-24), Preferred Sources (08-17), the generative-AI performance report check (08-06), and lock-screen photography (08-06). Everything else on this list has been resolved or superseded and is struck through.
- **The pipeline is genuinely alive again.** The routine pushed a branch on 08-31 and shipped p22 (citation-audit) — first autopilot output since 08-22. I verified that branch against the merge-gate rules myself (no source files touched, JSON valid, no first-person device claims, no raw Search Console counts) and merged it, rather than leaving it to sit while this refresh edited the same file — a same-file conflict is exactly how the last stall started.
- **THE HEADLINE — the "best free Bible app for iPhone" hub crossed onto page 1.** It had been stuck on page 2 for three cycles. It moved roughly a full page this window and took real clicks for the first time; the named head terms in the family each moved up about a page too. This is the biggest positional gain the site has recorded and it validates the depth method. I updated SEO-STRATEGY.md §2 and §3 Play 1 to match — the play is re-aimed at top-5, not closed.
- **Your brand KPI moved again, and this is the one to watch.** The one-word "faithwall" spelling now ranks first and converts a large share of what it sees, having been absent from the data entirely one window ago. That is precisely the leading indicator §8 predicts — and it is happening with *zero* off-site placements live, which makes p23 (your five pitches, still undrafted) the highest-leverage thing on this list. The two-word "faith wall" query stayed flat in conversion despite growing impressions, exactly as §3 Play 3 predicted; it stays classed as unconvertible and no homepage work will be queued for it.
- **No Google change needs anything from you.** The alleged "August 2026 core update" is confirmed *not real* — Google's own status dashboard shows only the August 18 spam update for the month, and our data covering that rollout shows no drop. Two genuinely new Google entries landed on 08-28 and neither touches us: the site-reputation-abuse enforcement carve-out is EEA-scoped and applies to sites hosting third-party content, which we do not; the favicon documentation now lists supported formats explicitly and our PNG favicon already complies.

## 2026-08-31 (p22 citation-audit)

- **NEW COMPETITOR FOUND — abbeybible.com ("Abbey").** Not in the SEO-STRATEGY §8 seed list. Its own site publishes self-ranked "best lock screen Bible app" / "best Bible widget app" roundups that place Abbey #1, ahead of FaithWall, on the exact lock-screen-app head-term family. Worth a quick look — same self-promotional pattern as FaithLock/Psalmo/Prayhouse, not an outreach target, but a competitor worth knowing about. See `outreach/targets.json`.
- **NETWORK CONSTRAINT — WebFetch to arbitrary domains is blocked from this cloud sandbox.** Every attempt to fetch a target page directly (bibleversedaily.org, warmpeach.com, abbeybible.com) returned `EGRESS_BLOCKED` from the network proxy. WebSearch itself still works and was used instead (search-result snippets + targeted follow-up queries), so this audit still shipped, but future citation-audit and outreach-prep runs will hit the same wall for any direct page read — worth knowing if a future item's contract ever requires exact on-page verification (e.g. confirming a submitted listing went live) rather than what a search snippet can show. Same class of restriction as the Unsplash API block noted 2026-08-16.
- **THREE NEW CANDIDATE OUTREACH TARGETS surfaced, not yet in §8's seed list:** thesweetsetup.com (established independent iOS app-review publication), thecripplegate.com (independent pastoral blog), margaretfeinberg.com (independent published-author blog). None drafted yet — flagging for a future `outreach-prep` wave after the current Tier-1 five (p23) ship. No action needed from you now, just visibility.
- **outreach/targets.json created** (first version) — the citation-audit output p22 produces. Confirms bibleversedaily.org and warmpeach.com (the two Tier-1 targets checked this pass) still do not mention FaithWall, reinforcing that p23's five drafts remain the highest-leverage open item.

## 2026-08-30 (merge gate — day 8: UNBLOCKED, and the trap that caused it is closed)

**Nothing needed from you. The two commands the last six notes asked for have been run, with your go-ahead.**

- **Pipeline is unblocked.** `origin/claude/seo-p21-audio-section` and `origin/work` — both pointing at the rejected commit `02fabaf` — are deleted. `git branch -r --no-merged main` now returns **empty**, so the routine's precondition is satisfied whatever its exact form (claude/* only, or any unmerged ref). Expect p22 (citation-audit) on the next run, then p23 (outreach-prep — the five pitch drafts §8 calls the binding constraint, still never executed).
- **Nothing was discarded.** Before deleting anything I pushed `02fabaf` to origin as the **tag** `rejected/p21-audio-section-02fabaf`, and verified it landed first. A tag is not a branch, so `git branch -r --no-merged main` cannot see it and it cannot trip the gate — while the commit stays permanently on the remote, independent of any clone. Recover it any time with `git checkout rejected/p21-audio-section-02fabaf`. The rejected text must still never be reused: p21 remains a fresh start from main.
- **The rejection itself was re-verified once more before quarantine and stands.** `src/data/posts/best-free-bible-app-iphone.json` asserts a timed on-device test — *"We started playback on each app, backed out to the home screen, and timed how long audio kept running"* — plus five more "in our test" / "confirmed background playback" claims. A cloud sandbox has no iPhone. Quarantined, never merged.
- **ROOT CAUSE CLOSED — `SEO-STRATEGY.md` §5.** This is the change to actually review. The "Effort signals (v2, mandatory)" rule demands two unfakeable signals and offers *"tested offline behavior"* and *"a dated hands-on observation"* among the four options. A device-less agent required to produce two, with no honest way to get them, invented them — the general "never fabricate" line lost to a specific **mandatory** requirement. I added a new §5 bullet that **outranks the two-signal minimum**: the cloud agent may never write a first-person testing/timing/device-behaviour claim, must satisfy the rule from what it can honestly source (App Store listing facts, publisher docs quoted and attributed, iOS setting paths, named-author judgment with reasoning, existing repo screenshots), and must write any genuinely needed device test as a TODO line here instead of inventing it. A page that cannot honestly reach two signals ships with one plus that TODO line. **Your standard is unchanged for work that has a real device behind it** — I constrained the agent, not the bar. If you would rather narrow §5 itself, that is still your call.
- **STRUCTURAL FIX — the merge gate no longer stalls on a bad branch.** The old rule said: on FAIL, leave the branch intact and report. That is what turned one rejected commit into eight days of zero output. The gate now *preserves-then-quarantines* — tag the commit, push the tag to origin, verify it landed, delete every remote ref at that SHA (including the sandbox's duplicate under a second name, which is what `work` was), and confirm the unmerged list is empty. Merging bad work is still forbidden; leaving a corpse on the remote no longer is.
- **DETECTION FIX — silence is no longer read as normal.** The gate previously treated "no branch today" as always benign, which is how eight dead mornings looked routine. It now checks the last FaithWall Bot commit date, and 3+ consecutive days of no output while the queue holds pending items is reported as "the routine is blocked, disabled, or erroring" at the top of the report — with the repo-side cause checked first, and an explicit instruction not to spend a second day asserting a repo-side cause it hasn't verified.
- **~~Still genuinely unknown~~ — ANSWERED 2026-08-31. The unmerged ref was the blocker.** With origin clean, the routine pushed `claude/seo-autopilot-20260831` the very next morning and shipped p22. No look at the run history needed; nothing is wrong with the routine. Eight days of silence were caused by exactly the precondition the notes inferred.

## 2026-08-24 (weekly refresh)

- **INFRA — Cloudflare is rewriting our robots.txt and it contradicts SEO-STRATEGY §7.** `public/robots.txt` in this repo is correct and unchanged, but the file actually served at `faithwall.app/robots.txt` is no longer that file. Cloudflare is injecting a "Managed content" block **above** ours that (a) sets content signals we never chose — `ai-train=no, use=reference` — and (b) issues `Disallow: /` for **GPTBot, ClaudeBot, CCBot, Google-Extended, Applebot-Extended, Bytespider, Amazonbot and meta-externalagent**. Our `Allow: /` groups still follow it, so a strictly RFC-9309-compliant crawler should merge the duplicate groups and let the equal-length `Allow` win, and the two crawlers that matter most for AI-search *citation* (**OAI-SearchBot** and **PerplexityBot**) are not in Cloudflare's list at all. I also verified there is no request-level block: GPTBot, OAI-SearchBot, ClaudeBot, PerplexityBot and Googlebot user-agents all get a normal `200` from the money page. **So this is a contradictory-signal problem, not a confirmed blocking problem — I am not going to tell you the sky is falling.** Two caveats keep it on this list anyway: my crawler test spoofs the user-agent from a normal IP, and Cloudflare verifies real bots by IP, so it does not fully rule out a verified-bot block; and Google Search itself is unaffected either way (Googlebot is not in the list). **What only you can do:** open the Cloudflare dashboard for faithwall.app → AI Crawl Control / Managed robots.txt, and decide deliberately what we want, because right now a default is deciding for us. My recommendation, matching §1 and §8: keep **search and AI-search crawlers allowed** (OAI-SearchBot, PerplexityBot, Googlebot, and ideally ClaudeBot and GPTBot for retrieval), and if you want to opt out of model *training* do it knowingly via the content signal rather than by a blanket disallow. Nothing was changed in the repo for this — the repo file is already right, and the override is above it.
- **~~The claude-seo repo has gone stale — recommend dropping it~~ — REVERSED 2026-08-31, do nothing.** It published again on 2026-08-25 (CHANGELOG 2.2.5) and its ledger now carries both changes it had missed. The revised read: it is a useful *lagging cross-check*, not a leading source — it published those entries a week after we had already verified them ourselves against Google's own feed. Keep it in the runbook as the cross-check it now is; `developers.google.com/search/updates` + `status.search.google.com` stay primary. No runbook edit needed, and nothing for you to decide.

## 2026-08-23 (merge gate — BRANCH REJECTED)

- **PIPELINE UNBLOCKED at the 2026-08-24 refresh — but your doctrine decision is still open.** I did not touch `SEO-STRATEGY.md` §5 for this, because narrowing the mandatory effort-signal list is your call, not mine. What I did instead, at the queue level: (1) rewrote **p21**'s notes with a hard, explicit constraint — no first-person testing, timing or device-behaviour claims from a cloud sandbox, an allow-list of effort signals it can genuinely source (App Store listing facts, publisher docs quoted and attributed, iOS setting paths, named-author judgement stated as reasoning), and an instruction to write any genuinely needed device test as a TODO line here rather than inventing it; and (2) demoted p21 behind **p22** (citation-audit) and **p23** (outreach-prep), which are research-and-drafting items that carry no fabrication risk at all. **The autopilot can therefore run tomorrow without touching the trap.** The branch `origin/claude/seo-p21-audio-section` is still intact and unmerged — the rejected text must not be reused; treat p21 as a fresh start from main. **~~Still yours to decide~~ — SUPERSEDED 2026-08-30 and confirmed working 2026-08-31.** The §5 device-less rule shipped in `b456222` and now outranks the two-signal minimum site-wide, so the trap is disarmed for every future `depth-pass` and `new-post`, not just p21. The first item to run under it (p22) produced no fabricated claim. Narrowing §5 itself remains available to you but is no longer needed.

## 2026-08-22 (strategy v2 overhaul)

- **STRATEGY v2 SHIPPED — read SEO-STRATEGY.md §8-§9.** Summary: on-page work plateaued (money pages upper page 1, further polish has diminishing returns); the binding constraint is site authority + brand recognition. The queue now produces outreach drafts, citation audits, and depth-passes instead of near-daily new posts (capped 2/week).
- **YOU are the send button — and as of 2026-08-24 the drafts still do not exist.** p23 has never run: it sat behind the rejected p21 branch for two days, so `outreach/` has not been created and not one pitch has been drafted. This is now the **first** thing the autopilot does (p22 → p23), so expect the five drafts (bibleversedaily.org, warmpeach.com, learnofchrist.com, healthyspirituality.org, biblegeeks.fm) within a couple of runs. Sending them is still the single highest-leverage 30 minutes available to this project. Worth knowing while you wait: the brand KPI moved this week for the first time in the right direction, which is the leading indicator §8 predicts.
- **Account-creation list (when you have an evening):** faith.tools submission, AlternativeTo (add FaithWall under Hallow/Glorify/YouVersion alternatives), SaaSHub, MochiLaunch. p27 will prep exact copy for each. Product Hunt / Indie Hackers are optional launch-moment plays — your call.
- **DECISION — owned audience.** The site converts nothing it can keep: no email capture, no community channel. Options: a light email capture ("verse pack of the month"), or leaning on the app itself as the owned channel. Product/brand call — yours.
- **DATA EXPORT WANTED — original data study.** The strongest citable asset we could own is aggregate, anonymized app data (e.g. most-set verses by month, verse-category popularity by season). Nobody in this niche has published anything like it. If you can export aggregate numbers (no user-level data), a `data-study` queue item turns it into a citable page.
- **App-side brand lever: App Store review velocity.** Review prompts timed to a genuine win-moment (e.g. after the 7th daily verse shown). App-side change — flagging, not prescribing.

## 2026-08-17 (weekly refresh)

- **DECISION — Google Preferred Sources. The implementation question is now answered; only the brand call is left.** Google extended Preferred Sources into AI Overviews and AI Mode (2026-05-27) and states people are roughly twice as likely to click through to a source they have marked as preferred. **New as of 2026-08-20 (verified against Google's own docs this refresh):** Google shipped an official **custom button** for it — a script tag plus a `<div google-add-preferred-source-btn></div>` element renders a Google-styled, auto-localized button, with an advanced JS API if we want to bind it to our own UI, and a plain deeplink alternative (`https://www.google.com/preferences/source?q=faithwall.app`). Domain-level sites are eligible, so faithwall.app qualifies in principle; the site must already appear in Google's source-preferences tool. So: (a) confirm we are eligible and appear in that tool — needs your Search Console/Google account; (b) decide whether we are willing to put a light "add FaithWall as a preferred source" prompt on the site or in the app. That is a brand/UX call, not an SEO one, which is why the autopilot has not touched `SEO-STRATEGY.md` for it. If you say yes, the code side is genuinely about two lines. Given that most of our impressions live in exactly the AI surfaces this operates in, this remains the highest-leverage earned-visibility lever on the list.

## 2026-08-16

- **Cloud session's outbound network policy blocks api.unsplash.com.** Every CONNECT to `api.unsplash.com` from the cloud sandbox returns `403` — confirmed via the proxy's own status endpoint as a policy denial, not a flaky request. Posts fall back to reusing an on-theme image per the runbook, so nothing breaks, but every future cloud-run post hits the same wall until Unsplash is allow-listed. Needs your access to the environment config. *Still open as of 2026-08-24 — though note that p22/p23/p29 (the next three items) need no new imagery, so this is not currently blocking anything.*

## 2026-08-06 (weekly refresh)

- **Check whether faithwall.app has the Search Console generative-AI performance report yet** (Search Console → Performance → Search, look for the AI Overviews / AI Mode breakdown). Google shipped it 2026-06-03 to a subset of properties first; impressions-only, data from 2026-05-18, no backfill. If we have it, the weekly dump should start pulling it — it is the only first-party read we can get on the AI-surface impressions §1 says are most of our visibility.
  - *Reinforced 2026-08-10, 2026-08-17, and again 2026-08-24 — still the highest-value item on this list.* About four impressions in five across the site carry no identifiable query. The five spokes hardened by p17 grew impressions again this window and **still show essentially zero named queries between them**, so the AEO work is being judged on raw impression growth alone and we cannot see citations at all. One useful new data point in the meantime: `/random-bible-verse` turns out to be about nine-tenths legible, which is why it is suddenly the most measurable page we own — and a reminder of how blind we are everywhere else. This report pairs directly with the Preferred Sources item above: same invisible surface.

## 2026-08-06

- **Photograph real lock screens of every app listed on `/best-bible-verse-lock-screen-apps`** (FaithWall, YouVersion, Glorify, Bible Gateway). No competitor in this niche has real device photography — the single biggest trust/differentiation lever available on the site's top page (SEO-STRATEGY.md §3 Play 2). Simulator screenshots won't read as authentic; needs actual iPhone captures. *Re-flagged 2026-08-22 as a §5 effort-signal requirement, and now doubly relevant: the p21 fabrication incident happened precisely because the agent cannot produce first-hand device evidence and the doctrine demanded it. Your photos are the clean fix for the whole class of problem.*
